module.exports = {
  title: 'perfectSymphony',

  /**
   * 是否需要设置右侧面板
   */
  showSetting: true,

  /**
   * 是否需要标签
   */
  tagsView: true,

  /**
   * 是否固定头部
   */
  fixedHeader: true,

  /**
   * 是否在侧导航显示项目logo
   */
  sidebarLogo: true,

  /**
   * 显示组件的报错日志
   * 默认是仅仅用在生产环境
   * 如果想要使用在开发环境， 可以通过设置['production', 'development']
   */
  errorLog: 'development'
}
